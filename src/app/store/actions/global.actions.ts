import { ApiActions } from './api.actions';

export const GlobalActions = {
    Api: {
        UpdateUsers: ApiActions.UpdateUsers(),
        UpdateUserProfile: ApiActions.UpdateUserProfile(),
    }
}
